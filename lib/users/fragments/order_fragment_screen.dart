import 'package:flutter/material.dart';

class OrderFragmentScreen extends StatelessWidget {
  const OrderFragmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Order Fragments Screen"),
      ),
    );
  }
}
